# PyInstaller build output — regenerated by build_mac.sh / build_windows.bat / CI
build/
dist/
*.spec

# Python
__pycache__/
*.pyc

# App runtime data (created automatically in ~/.smart-file-organizer, never here)
logs/

# OS junk
.DS_Store
